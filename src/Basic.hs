module Basic where

-- First try running the test suites with this function as "undefined".
-- Then fill in the proper function definition and see that the tests pass!
-- simpleMathFunction a b c = a * b - c
simpleMathFunction :: Int -> Int -> Int -> Int
simpleMathFunction a b c = undefined